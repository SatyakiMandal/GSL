.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Scraped HTML, price JSON and the provenance log are run artefacts.
# Keep the directory, not its contents.
cache/*
!cache/.gitkeep

# Machine-readable probe output; the narrative lives in docs/phase0-findings.md
docs/phase0-*.json
data/prices/*.csv
out/

# The news fixture in data/ is committed deliberately (bodies stripped).
!data/adani_wide_2023.json
